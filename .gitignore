# Personal database dumps — real user data, never publish
SQL files/

# OS / editor cruft
Thumbs.db
.DS_Store

# Supabase CLI local state (project link cache, generated by
# `supabase login`/`functions deploy`, not a secret but not part of
# the repo either — same thing Supabase's own scaffolding gitignores)
supabase/.temp/
